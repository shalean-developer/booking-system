'use client';

/**
 * Admin bookings list (authenticated admin session required).
 * For customer-facing history, use dashboard APIs or `app/dashboard` data fetching.
 */
export { useAdminBookings as useBookings } from './use-admin-bookings';
