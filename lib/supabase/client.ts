/**
 * Canonical browser Supabase client for the Next.js app.
 * Re-exports the singleton from `lib/supabase-client.ts` (createBrowserClient + PKCE).
 */
export { supabase } from '../supabase-client';
