/** Target baseline for time-based fairness (cents per hour). R40/hour = 4000 cents. */
export const TARGET_HOURLY_RATE = 4000;

/** Upper bound for admin “overpay risk” warnings (cents per hour). R120/hour. */
export const MAX_HOURLY_RATE = 12_000;
