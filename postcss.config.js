module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
    // Next.js handles CSS minification via SWC, so we don't need cssnano
    // This keeps the config simple and avoids conflicts
  },
}

